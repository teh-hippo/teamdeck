name: Security Audit

on:
  push:
    branches: [main]
    paths: ["native/**"]
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch:

concurrency:
  group: security-audit-${{ github.ref }}
  cancel-in-progress: true

jobs:
  audit:
    uses: teh-hippo/common-repo-configs/.github/workflows/security-audit.yml@0cefd797b02a13469434b002d89a787487349670  # v3.2.22
    permissions:
      contents: read
      issues: write
      checks: write
    with:
      working-directory: native
